name: ids_monitor
description: Intelligent IDS Pipeline Monitor

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.1.1
  flutter_dotenv: ^5.1.0
  syncfusion_flutter_gauges: ^21.1.35
  cryptography: ^2.9.0
  hex: ^0.2.0
  pointycastle: ^3.7.3
  crypto: ^3.0.3
  flutter_hooks: ^0.20.4
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
