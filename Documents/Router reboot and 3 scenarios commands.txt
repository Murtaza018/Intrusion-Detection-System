after router reboot:
ip link set eth1 up
ip addr add 203.0.113.1/24 dev eth1
ip link set eth2 up
ip addr add 192.168.50.1/24 dev eth2
sysctl -w net.ipv4.ip_forward=1
nft flush ruleset 2>/dev/null || iptables -F



scenario 1(inside subnet):
in Inside-Pc-1 terminal:
ping 203.0.113.10

scenario 2(outside subnet):
In Kali Linux VM terminal:
sudo hping3 -S --flood -V 192.168.50.10

scenario 3(DMZ):
run E:\Intrusion-Detection-System\Backend\AI Model\Check_models\pivot_attack.py